function findNb(m) {
    // total volume = m -> forumal pile of cubes 
    //Bottom have n^3 above that is (n-1)^3 then the top is 1^3
    // if no such n return - 1
   return m = n^3 + (n-1)^3 + 1^3;
    //return (-1);
}

findNb(4183059834009);